const TodoFilters = () => {
  return (
    <div className="todo-filters">
      <button>All</button>
      <button>Active</button>
      <button>Completed</button>
    </div>
  );
};

export default TodoFilters;
